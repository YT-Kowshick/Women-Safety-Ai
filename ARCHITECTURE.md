# 🏗️ Women Safety AI - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React + Vite)                     │
│                         Port: 5173                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │  Dashboard   │  │ SafetyCheck  │  │  Simulation  │             │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘             │
│         │                 │                 │                      │
│         └─────────────────┼─────────────────┘                      │
│                           │                                        │
│                    ┌──────▼────────┐                               │
│                    │  useApi Hooks │                               │
│                    │  (Loading +   │                               │
│                    │   Errors)     │                               │
│                    └──────┬────────┘                               │
│                           │                                        │
│                    ┌──────▼────────┐                               │
│                    │  API Service  │                               │
│                    │   (api.ts)    │                               │
│                    └──────┬────────┘                               │
│                           │                                        │
└───────────────────────────┼─────────────────────────────────────────┘
                            │
                            │ HTTP/JSON Requests
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      BACKEND (FastAPI)                              │
│                      Port: 8000                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    CORS Middleware                            │  │
│  │        (Allows frontend to make cross-origin requests)        │  │
│  └──────────────────────┬───────────────────────────────────────┘  │
│                         │                                           │
│  ┌──────────────────────▼───────────────────────────────────────┐  │
│  │                   FastAPI Endpoints                           │  │
│  │  ┌──────────────────────────────────────────────────────┐    │  │
│  │  │  GET  /health           → Health Check               │    │  │
│  │  │  POST /predict/safety   → State + Year Prediction    │    │  │
│  │  │  POST /predict/simulate → What-If Simulation         │    │  │
│  │  │  GET  /trends           → Crime Trends Over Time     │    │  │
│  │  │  GET  /leaderboard      → State Rankings             │    │  │
│  │  └──────────────────────────────────────────────────────┘    │  │
│  └──────────────────────┬───────────────────────────────────────┘  │
│                         │                                           │
│  ┌──────────────────────▼───────────────────────────────────────┐  │
│  │              Pydantic Validation Models                       │  │
│  │     (SafetyRequest, SimulateRequest, Responses, etc.)        │  │
│  └──────────────────────┬───────────────────────────────────────┘  │
│                         │                                           │
│  ┌──────────────────────▼───────────────────────────────────────┐  │
│  │                   Business Logic                              │  │
│  │     • Feature engineering (crime ratios)                      │  │
│  │     • Data preparation                                        │  │
│  │     • Model prediction                                        │  │
│  │     • Risk level calculation                                  │  │
│  └──────────────────────┬───────────────────────────────────────┘  │
│                         │                                           │
└─────────────────────────┼───────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────┐        ┌──────────────────────┐          │
│  │  CrimesOnWomenData   │        │   safety_model.pkl   │          │
│  │       .csv           │        │   (ML Model)         │          │
│  │                      │        │                      │          │
│  │  • 2001-2021 data    │        │  • Trained on        │          │
│  │  • All Indian states │        │    historical data   │          │
│  │  • 7 crime types     │        │  • Predicts safety   │          │
│  │  • 14,000+ records   │        │    score (0-100)     │          │
│  └──────────────────────┘        └──────────────────────┘          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Example

### Example: Get Safety Score for Tamil Nadu, 2021

```
1. USER ACTION
   └─> Clicks "Check Safety" button in frontend

2. FRONTEND (React Component)
   └─> Calls: checkSafety("Tamil Nadu", 2021)
       ├─> Hook: useSafetyScore()
       │   └─> Sets: loading = true
       │
       └─> Service: getSafetyScore("Tamil Nadu", 2021)
           └─> fetch("http://localhost:8000/predict/safety", {
                 method: "POST",
                 body: JSON.stringify({ state: "Tamil Nadu", year: 2021 })
               })

3. NETWORK
   └─> HTTP POST Request
       └─> Headers: Content-Type: application/json
       └─> Body: { "state": "Tamil Nadu", "year": 2021 }

4. BACKEND (FastAPI)
   └─> CORS Middleware
       └─> Validates origin ✓
   
   └─> Route: POST /predict/safety
       └─> Pydantic Validation
           └─> Validates state (string) ✓
           └─> Validates year (2001-2025) ✓
       
       └─> Business Logic
           └─> Query dataset for matching row
           └─> Calculate crime ratios
           └─> Prepare features for ML model
           └─> model.predict(features)
           └─> Calculate risk level from score
       
       └─> Response
           └─> {
                 "state": "Tamil Nadu",
                 "year": 2021,
                 "safety_score": 41.3,
                 "risk_level": "Medium"
               }

5. NETWORK
   └─> HTTP 200 OK
       └─> Body: JSON response

6. FRONTEND (React Component)
   └─> Service receives response
       └─> Hook updates state:
           ├─> loading = false
           ├─> data = { state: "Tamil Nadu", ... }
           └─> error = null
   
   └─> Component re-renders
       └─> Displays:
           "Tamil Nadu - 2021"
           "Safety Score: 41.3"
           "Risk Level: Medium" (orange color)
```

---

## 📁 File Relationship Map

```
Project Root
│
├── Frontend Files
│   │
│   ├── src/hooks/useApi.ts
│   │   └── Imports: src/services/api.ts
│   │   └── Exports: React hooks with state management
│   │   └── Used by: Page components (Dashboard, SafetyCheck, etc.)
│   │
│   ├── src/services/api.ts
│   │   └── Calls: Backend API endpoints
│   │   └── Exports: API functions + types
│   │   └── Used by: useApi hooks
│   │
│   └── .env
│       └── Defines: VITE_API_URL=http://localhost:8000
│       └── Used by: api.ts for base URL
│
├── Backend Files
│   │
│   ├── app.py
│   │   └── Imports: pandas, joblib, fastapi
│   │   └── Loads: CrimesOnWomenData.csv, safety_model.pkl
│   │   └── Exports: FastAPI app with 5 endpoints
│   │   └── Serves: API on port 8000
│   │
│   ├── CrimesOnWomenData.csv
│   │   └── Contains: Historical crime data (2001-2021)
│   │   └── Used by: app.py for data lookup
│   │
│   ├── safety_model.pkl
│   │   └── Contains: Trained ML model
│   │   └── Used by: app.py for predictions
│   │
│   └── test_api.py
│       └── Tests: All 5 API endpoints
│       └── Calls: http://localhost:8000/*
│
└── Documentation
    │
    ├── README.md (Overview)
    ├── INTEGRATION_GUIDE.md (How to connect)
    ├── QUICK_REFERENCE.md (Cheat sheet)
    ├── COMPLETION_SUMMARY.md (Status report)
    └── backend/API_DOCUMENTATION.md (API reference)
```

---

## 🔌 API Endpoint Details

```
┌─────────────────────────────────────────────────────────────────┐
│  Endpoint: GET /health                                          │
├─────────────────────────────────────────────────────────────────┤
│  Input:  None                                                   │
│  Output: { "status": "ok" }                                     │
│  Use:    Verify server is running                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Endpoint: POST /predict/safety                                 │
├─────────────────────────────────────────────────────────────────┤
│  Input:  { "state": string, "year": number }                    │
│  Output: {                                                      │
│            "state": string,                                     │
│            "year": number,                                      │
│            "safety_score": number,                              │
│            "risk_level": "Low" | "Medium" | "High"              │
│          }                                                      │
│  Use:    Get safety score for existing state + year             │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Endpoint: POST /predict/simulate                               │
├─────────────────────────────────────────────────────────────────┤
│  Input:  {                                                      │
│            "year": number,                                      │
│            "rape": number,                                      │
│            "kidnapping": number,                                │
│            "dowry_deaths": number,                              │
│            "assault_on_women": number,                          │
│            "assault_on_minors": number,                         │
│            "domestic_violence": number,                         │
│            "trafficking": number                                │
│          }                                                      │
│  Output: {                                                      │
│            "safety_score": number,                              │
│            "risk_level": "Low" | "Medium" | "High"              │
│          }                                                      │
│  Use:    What-if analysis with custom crime data                │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Endpoint: GET /trends?state=...&crime=...                      │
├─────────────────────────────────────────────────────────────────┤
│  Input:  Query params: state (string), crime (string)           │
│  Output: {                                                      │
│            "state": string,                                     │
│            "crime": string,                                     │
│            "data": [                                            │
│              { "year": number, "value": number },               │
│              ...                                                │
│            ]                                                    │
│          }                                                      │
│  Use:    Get historical crime trends for charting               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Endpoint: GET /leaderboard?year=...                            │
├─────────────────────────────────────────────────────────────────┤
│  Input:  Query param: year (optional)                           │
│  Output: [                                                      │
│            { "state": string, "score": number },                │
│            ...                                                  │
│          ]                                                      │
│  Use:    Get states ranked by safety (lower score = safer)      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎨 Technology Stack Visualization

```
Frontend Layer
├── React 18          (UI Framework)
├── TypeScript        (Type Safety)
├── Vite              (Build Tool)
├── Tailwind CSS      (Styling)
└── Shadcn/ui         (UI Components)

API Layer (Custom)
├── api.ts            (Service Functions)
├── useApi.ts         (React Hooks)
└── .env              (Configuration)

Backend Layer
├── FastAPI           (Web Framework)
├── Uvicorn           (ASGI Server)
├── Pydantic          (Validation)
└── CORS Middleware   (Security)

Data Layer
├── Pandas            (Data Manipulation)
├── Scikit-learn      (ML Model)
├── Joblib            (Serialization)
└── CSV               (Data Storage)
```

---

## 🚀 Deployment Flow

```
Development:
  Frontend: localhost:5173
  Backend:  localhost:8000
  CORS:     allow_origins=["*"]

Production:
  Frontend: https://womensafety.app
  Backend:  https://api.womensafety.app
  CORS:     allow_origins=["https://womensafety.app"]

Deployment Steps:
  1. Update .env → VITE_API_URL=https://api.womensafety.app
  2. Update app.py → allow_origins=["https://womensafety.app"]
  3. Deploy backend → Render/Railway/AWS
  4. Deploy frontend → Vercel/Netlify
  5. Test endpoints → Use test_api.py with production URL
```

---

**Visual Architecture Complete! 🎨**
