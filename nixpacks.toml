[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "python -m pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "bash -lc \"cd backend && python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}\""
